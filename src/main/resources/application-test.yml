spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/content_db_test
    username: postgres
    password: postgres

  flyway:
    enabled: true
    clean-disabled: false

  kafka:
    bootstrap-servers: localhost:9092

  data:
    redis:
      host: localhost
      port: 6379

passtheo:
  strapi:
    base-url: http://localhost:1337
  subscription-service:
    base-url: http://localhost:8083

logging:
  level:
    root: WARN
    com.passtheo: DEBUG
