apiVersion: v1
kind: Service
metadata:
  name: {{ include "passtheo-content-service.fullname" . }}
  labels:
    {{- include "passtheo-content-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "passtheo-content-service.selectorLabels" . | nindent 4 }}
