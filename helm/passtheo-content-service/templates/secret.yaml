apiVersion: v1
kind: Secret
metadata:
  name: {{ include "passtheo-content-service.fullname" . }}-secret
  labels:
    {{- include "passtheo-content-service.labels" . | nindent 4 }}
type: Opaque
data:
  {{- range $key, $value := .Values.secrets }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
